#- VIC020 Unit Tests - Copyright 2026 Fabio Carignano ------------------------
#
# This file is a modified version of the original 'pet.cfg' from the cc65 
# distribution (https://github.com/cc65/cc65)
#
SYMBOLS {
    __LOADADDR__:  type = import;
    __EXEHDR__:    type = import;
    __STACKSIZE__: type = weak, value = $0800; # 2k stack
}
MEMORY {
    # added 4 bytes to cc65's default size of the zero page memory area
    ZP:       file = "", define = yes, start = $0055, size = $001E;
    LOADADDR: file = %O,               start = $03FF, size = $0002;
    HEADER:   file = %O,               start = $0401, size = $000C;
    RAM:      file = %O,               start = $040D, size = $7BF3 - __STACKSIZE__;
}
SEGMENTS {
    ZEROPAGE: load = ZP,       type = zp;
    LOADADDR: load = LOADADDR, type = ro;
    EXEHDR:   load = HEADER,   type = ro;
    STARTUP:  load = RAM,      type = ro;
    LOWCODE:  load = RAM,      type = ro,  optional = yes;
    ONCE:     load = RAM,      type = ro,  optional = yes;
    CODE:     load = RAM,      type = ro;
    RODATA:   load = RAM,      type = ro;
    DATA:     load = RAM,      type = rw;
    INIT:     load = RAM,      type = bss;
    BSS:      load = RAM,      type = bss, define   = yes;
}
FEATURES {
    CONDES: type    = constructor,
            label   = __CONSTRUCTOR_TABLE__,
            count   = __CONSTRUCTOR_COUNT__,
            segment = ONCE;
    CONDES: type    = destructor,
            label   = __DESTRUCTOR_TABLE__,
            count   = __DESTRUCTOR_COUNT__,
            segment = RODATA;
    CONDES: type    = interruptor,
            label   = __INTERRUPTOR_TABLE__,
            count   = __INTERRUPTOR_COUNT__,
            segment = RODATA,
            import  = __CALLIRQ__;
}
